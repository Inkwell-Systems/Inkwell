import React from 'react';
import {
    EditorElementContainer,
    EditorSectionContainer,
    EditorSectionTitle,
} from '../utils.tsx';

const SpreadsheetView = () => {
    return (
        <EditorElementContainer>
            <EditorSectionTitle>Spreadsheet View</EditorSectionTitle>
            <EditorSectionContainer>
                <p>The spreadsheet view is still a work-in-progress.</p>
            </EditorSectionContainer>
        </EditorElementContainer>
    );
};

export default SpreadsheetView;
