import React from 'react';
import {
    EditorElementContainer,
    EditorSectionContainer,
    EditorSectionTitle,
} from '../utils.tsx';

const DiagramView = () => {
    return (
        <EditorElementContainer>
            <EditorSectionTitle>Diagram view</EditorSectionTitle>
            <EditorSectionContainer>
                <p>Diagram view is still in development.</p>
            </EditorSectionContainer>
        </EditorElementContainer>
    );
};

export default DiagramView;
